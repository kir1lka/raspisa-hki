# Домен берётся из переменной DOMAIN (по умолчанию localhost для локального теста).
# Для реального домена Caddy САМ получит HTTPS-сертификат Let's Encrypt.
{$DOMAIN:localhost} {
	# Все запросы (сайт, API, статика) — в наше приложение
	reverse_proxy app:8080
}
